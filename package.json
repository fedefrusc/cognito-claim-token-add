{
  "name": "cognito-claim-token-add",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "deploy-stag": "AWS_PROFILE=tag-stag serverless deploy --stage stag",
    "deploy-prod": "AWS_PROFILE=tag-prod serverless deploy --stage prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless": "^3.38.0"
  }
}
